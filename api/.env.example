# ==========================
# TaskPilot API - Environment Variables
# ==========================

# Server Configuration
PORT=4000
NODE_ENV=development

# MongoDB Connection
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/taskpilot?retryWrites=true&w=majority

# Auth0 Configuration
# Option 1: Provide the full issuer URL (recommended)
AUTH0_ISSUER=https://your-tenant.auth0.com/

# Option 2: Provide just the domain (will be prefixed with https://)
# AUTH0_DOMAIN=your-tenant.auth0.com

# Auth0 API Audience (identifier for your API in Auth0 dashboard)
AUTH0_AUDIENCE=https://taskpilot-api

# Development-Only Settings
# Set to 'true' to allow mock role/tenant injection in non-production environments
ALLOW_MOCK_AUTH=false

# Google Gemini AI (Optional)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ==========================
# Setup Instructions
# ==========================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update MONGO_URI with your MongoDB Atlas connection string
# 3. Create an Auth0 account and API at https://auth0.com
# 4. Set AUTH0_ISSUER to your Auth0 tenant URL (e.g., https://dev-abc123.us.auth0.com/)
# 5. Set AUTH0_AUDIENCE to your API identifier from Auth0 dashboard
# 6. In production, ensure NODE_ENV=production and ALLOW_MOCK_AUTH=false
